<%= form_for :query, url: searches_path, method: :get do |form| %>
  <%= text_field_tag :query, params[:query], id: "ex1", class: "form-control", placeholder: t(".placeholder") %>
  <%= form.submit t(".search"), class: "btn btn-primary" %>
<% end %>
